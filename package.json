{
  "name": "sandbox",
  "version": "2.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "clean": "rimraf _site",
    "start": "npx @11ty/eleventy --serve --quiet"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0",
    "rimraf": "^4.1.2"
  }
}
